import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.triSeguros.starter',
  appName: 'Tri seguros',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
